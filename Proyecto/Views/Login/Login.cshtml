@{
    ViewBag.Title = "Login";
}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <title>@ViewData["Title"] - Mi Aplicación</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet" />
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            background-color: #f4f4f9;
        }

        .sidebar {
            width: 250px;
            height: 100vh;
            background-color: #2c3e50;
            position: fixed;
            padding-top: 20px;
        }

            .sidebar ul {
                list-style-type: none;
                padding: 0;
            }

                .sidebar ul li {
                    text-align: left;
                    margin: 15px 0;
                }

                    .sidebar ul li a {
                        display: flex;
                        align-items: center;
                        color: #ecf0f1;
                        padding: 10px 20px;
                        text-decoration: none;
                        font-size: 16px;
                        transition: background-color 0.3s ease;
                    }

                        .sidebar ul li a:hover {
                            background-color: #34495e;
                            border-radius: 5px;
                        }

                        .sidebar ul li a i {
                            margin-right: 10px;
                        }

        .main-content {
            margin-left: 250px;
            padding: 40px;
            background-color: #fff;
            min-height: 100vh;
        }

            .main-content h1 {
                color: #2c3e50;
            }
    </style>
</head>
<body>
    <div class="sidebar">
        <ul>
            <li><a href="@Url.Action("Index", "Home")"><i class="fas fa-home"></i> Inicio</a></li>
            <li><a href="@Url.Action("GestionarNegocio", "Negocio")"><i class="fas fa-briefcase"></i> Gestionar Negocio</a></li>
            <li><a href="@Url.Action("Index", "Estadistica")"><i class="fas fa-chart-line"></i> Estadísticas</a></li>
            <li><a href="@Url.Action("Reportes", "Reportes")"><i class="fas fa-file-alt"></i> Reportes</a></li>
            <li><a href="@Url.Action("Configuraciones", "Configuraciones")"><i class="fas fa-cogs"></i> Configuraciones</a></li>
            <li><a href="@Url.Action("Logout", "Login")"><i class="fas fa-sign-out-alt"></i> Cerrar Sesión</a></li>
        </ul>
    </div>

    <div class="main-content">
        <h1>Bienvenido a la aplicación</h1>
        <p>Este es tu panel principal. Usa el menú lateral para navegar por las diferentes secciones.</p>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>